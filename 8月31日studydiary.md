日期:8月31日

李政慷

知识点依旧不是很多

今日完成:
1.红外+感器大结合
2.PID终于完成了

遇到问题及解决方案:
1.全传感器蛮简单的,就是OLED的排版要花点时间,老是显示不全,或者两行重合导致看不清.但整体来说全传感器只用了今天40%的时间


2.PID终于找到解法了,
第一个问题,轮子反向,
主要是因为里程差纠偏将补偿量 `steer` 反向叠加到两轮（`pwm_left -= steer; pwm_right += steer`）。当 `dist_error` 达到限幅（实测 `dist=-800`，`steer=-40`）且纠偏量大于该轮前馈与比例项之和时，单轮 PWM 被压成负值，出现单轮反转（实测 `PWM_A=297 PWM_B=-526`，一正一负）；
PI 积分 / 误差方向处理错误：若误差定义或积分累加方向与电机转向不匹配，反馈变成正反馈，控制器命令与实际运动互相强化。

而第二个问题,震荡
纯比例（P）控制无积分、无微分：P 控制无法消除稳态误差；无微分环节无法对速度变化提供阻尼，系统对扰动敏感，易在目标值附近往复；
起步动力不足：纯 PI 无前馈时，起步 PWM 仅等于误差 × 比例系数（实测起步 `PWM≈133`），远小于电机起转所需（实测 `PWM≈2000`），轮子在静摩擦临界点附近反复 "动 — 停 — 动"。

之后采用"前馈 + 比例 + 积分（含抗积分饱和）"结构：前馈随目标速度变化提供起步动力，比例项抑制扰动，积分项消除稳态误差并限幅防饱和；

最终完成了PID,走路走得非常直!!!

3.今天借的这个组的小车,3861板块的串口也爆炸了,整个 I2C0 总线上没有任何从设备在应答,串口无法输出东西.所以现在我在用三个小组的车

我们小组的车:3861无法烧录,但轮子好
2小组的车:3861正常,但轮子是歪的,导致我昨天PID卡了很久
3小组的车(今天用的车):3861可以烧录但串口无法输出,但轮子特别正,帮我完成了PID的搭建

我看前面那个小组已经在弄路径跟踪小车了,下一步也来试一试这个