李政慷

日期:8月25日
时长:3+3=6小时

学习内容:
小车跑马灯程序编辑
Ubuntu虚拟机网络连接
VSCode远程连接打开文件

知识点:
ST-LINK Utility来eraser代码


Linux常见快捷键命令
pwd                 # 查看当前所在路径
ls                  # 列出当前目录文件
ls -l               # 详细信息列表
ls -a               # 显示全部，包含隐藏文件
cd 文件夹名          # 进入文件夹
cd ..               # 返回上一级目录
cd ~                # 回到家目录
mkdir test          # 创建文件夹test
rm 文件名           # 删除文件
rm -r 文件夹名       # 删除文件夹（递归删除）
cp 源文件 目标位置   # 复制文件
mv 源 目标          # 移动/重命名文件
cat 文件名          # 查看文件全部内容
nano 文件名         # 简单文本编辑器（写md日志用这个）
ip a                # 查看网卡IP地址
ping 地址           # 测试网络连通
sudo dhclient ens33 # 网卡重新获取IP
systemctl restart NetworkManager # 重启网络服务
sudo 命令           # 以管理员权限执行命令
`Ctrl+Alt+T` 打开终端


学习过程:
先跟着老师完成了Windows连接虚拟机和vscode的远程连接
然后不断调试跑马灯的代码

遇到问题:
1.虚拟机怎么都无法打开,战线长达两天,最后升级了一下VMware到17pro版本,才打开了虚拟机
2.虚拟机查不到IP地址,后面在ai帮助下,设置了net连接,才弄到虚拟机的IP地址
3.小车代码改完之后经常不生效,发现是while死循环导致的问题
4.不知道怎么上传代码到github仓库里面,只能把代码直接拖进去,不知道老师能不能预览查看

实验结果:
下课时,完成了Ubuntu虚拟机网络连接,VSCode远程连接打开文件
最后在原有代码的基础上设计了一个彩色跑马灯(只不过感觉还有一点点问题,后续再完善一下)

收获:
深刻理解了软件和系统之间的兼容性问题!
学会排查无人车运行出现的问题



